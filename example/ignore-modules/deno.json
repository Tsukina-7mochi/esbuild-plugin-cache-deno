{
  "tasks": {
    "build": "deno run -A build.ts",
    "cache": "rm -f deno.lock && deno cache **/*.ts",
    "clean": "rm -f bundle.js",
    "run:bundle": "deno run bundle.js",
    "run:raw": "deno run index.ts"
  }
}
